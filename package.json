{
  "name": "aliveawake-instagram-publisher",
  "version": "1.0.0",
  "private": true,
  "description": "Publish videos (with caption + thumbnail) to the AliveAwake Instagram account via the Instagram Graph API.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "publish": "node publish.js",
    "schedule:reel": "node scripts/schedule-reel.mjs",
    "audit:composio": "node scripts/audit-composio-callers.mjs",
    "test": "node --test scripts/lib/*.test.mjs && npm run audit:composio"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
